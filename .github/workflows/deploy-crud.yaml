name: Deploy crud function

permissions: {}

on:
  push:
    branches:
      - main
    paths:
      - 'functions/crud/**'
      - '.github/workflows/deploy-crud.yaml'

jobs:
  deploy:
    name: Deploy function
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: aws-actions/configure-aws-credentials@ec61189d14ec14c8efccab744f656cffd0e33f37 # v6.1.0
        with:
          aws-region: ${{ vars.AWS_REGION }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      - run: sudo apt-get update && sudo apt-get install -y zip
      - run: zip -j function.zip *.mjs
        working-directory: ./functions/crud
      - run: |
          aws lambda update-function-code \
            --function-name cucumber-reports-crud-prod \
            --zip-file fileb://$(pwd)/functions/crud/function.zip
